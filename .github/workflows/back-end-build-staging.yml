name: Back-End Staging Build
on:
  push:
    branches:
      - develop
    paths:
      - "back-end/**"
      - ".github"

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - uses: aws-actions/setup-sam@v1
      - uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: "eu-west-1"
      # sam build 
      - run: sam build --use-container

# Run Unit tests- Specify unit tests here 

# sam deploy
      - run: sam deploy --no-confirm-changeset --no-fail-on-empty-changeset --stack-name exercise-challenge-back-end --s3-bucket aws-sam-cli-managed-default-samclisourcebucket-h7wu99b350u0 --capabilities CAPABILITY_IAM --region eu-west-1 